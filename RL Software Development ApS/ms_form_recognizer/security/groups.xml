<?xml version="1.0"?>
<odoo>
    <data>

        <record model="res.groups" id="administrator">
            <field name="name">MS Form Recognizer - administrator</field>
            <field name="users" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
        </record>

        <record model="res.groups" id="user">
            <field name="name">MS Form Recognizer - user</field>
            <field name="users" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
        </record>

        
        
    </data>
</odoo>
